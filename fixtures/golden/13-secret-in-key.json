{
  "id": "secret-in-key",
  "description": "A secret can hide in the KEY position, not only a value: payload { 'sk-...': 'x' }. It must be redacted so the secret-shaped key name is never reflected back (e.g. via the response's forwardedPayloadKeys).",
  "env": { "GATEWAY_SIGNING_SECRET": "gateway-test-secret", "GATEWAY_NOW_SECONDS": 1000000, "GATEWAY_REPLAY_WINDOW_SECONDS": 300, "GATEWAY_MAX_BODY_BYTES": 65536 },
  "request": { "intent": "support-triage", "payload": { "sk-ABCDEFGH12345678": "x", "subject": "ok" }, "timestamp": 1000000, "sign": true },
  "expect": { "statusCode": 200, "ok": true, "routedTo": ["support-triage"], "strippedCountMin": 1, "noSecretInOutput": true, "traceIdNonEmpty": true }
}
