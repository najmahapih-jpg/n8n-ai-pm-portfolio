{
  "id": "secret-in-requestid",
  "description": "A secret-shaped requestId (client-controlled) must NOT be reflected into the traceId, response, or audit. The whole envelope is stripped and the traceId falls back to a hash (the requestId fails the safe-charset check after redaction).",
  "env": { "GATEWAY_SIGNING_SECRET": "gateway-test-secret", "GATEWAY_NOW_SECONDS": 1000000, "GATEWAY_REPLAY_WINDOW_SECONDS": 300, "GATEWAY_MAX_BODY_BYTES": 65536 },
  "request": { "intent": "support-triage", "payload": { "subject": "ok" }, "requestId": "sk-ABCDEFGH12345678", "timestamp": 1000000, "sign": true },
  "expect": { "statusCode": 200, "ok": true, "routedTo": ["support-triage"], "strippedCountMin": 1, "noSecretInOutput": true, "traceIdNonEmpty": true }
}
