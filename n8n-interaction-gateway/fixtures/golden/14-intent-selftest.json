{
  "id": "intent-selftest",
  "description": "The gateway-selftest intent resolves to the callable selftest sibling. OFFLINE this exercises the routing DECISION (routedTo == [gateway-selftest-sibling]); the actual in-process Execute-Workflow execution is proven by verify:live (executeWorkflow cannot run offline).",
  "env": { "GATEWAY_SIGNING_SECRET": "gateway-test-secret", "GATEWAY_NOW_SECONDS": 1000000, "GATEWAY_REPLAY_WINDOW_SECONDS": 300, "GATEWAY_MAX_BODY_BYTES": 65536 },
  "request": { "intent": "gateway-selftest", "payload": { "subject": "system is down", "message": "outage" }, "timestamp": 1000000, "sign": true },
  "expect": { "statusCode": 200, "ok": true, "routedTo": ["gateway-selftest-sibling"], "routedToLength": 1, "traceIdNonEmpty": true }
}
