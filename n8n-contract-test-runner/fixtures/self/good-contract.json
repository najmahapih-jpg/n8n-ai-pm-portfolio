{
  "description": "Well-formed contract aligned with the deployed workflow text and its fixtures -> all 5 offline checks PASS.",
  "fakeWorkflowText": "code node policyVersion demo-v1.0.0 ; webhook node path webhook/demo ; end",
  "fakeFixtures": [ { "name": "valid-1", "keys": ["a", "b"] } ],
  "contract": {
    "contractVersion": "demo-v1.0.0",
    "webhookPath": "webhook/demo",
    "request": { "accepted": ["a", "b", "c"], "limits": { "bodyBytes": 1024 }, "limitsEnforcedBy": "gateway" },
    "response": { "required": ["ok"] }
  }
}
