{
  "schemaVersion": 1,
  "slug": "autonomous-agent",
  "title": "Portfolio - Autonomous Agent",
  "description": "Capstone C -- a bounded tool-use agent. POST a signal -> Normalize Task -> Agent Loop (MIRRORS scripts/lib/agent-core.mjs: a deterministic keyword planner + refusal/allowlist/max-steps/no-fabricated guardrails, over STUB tools) -> respond with the run record (the trajectory + outcome). The agent's TOOLS are the portfolio workflows reached via the signed interaction-gateway; v0.1.0 runs the stub planner + stub tools (byte-stable trajectory), with a live LLM planner + live gateway tool execution as the opt-in next increments. The agent-core is the single source of truth (verify:agent 39/39); the Agent Loop Code node mirrors it inline, and scripts/test-agent-workflow.mjs proves the COMPILED copy (golden tasks + a DIFFERENTIAL asserting byte-identical run records vs the core). Deployed via the n8n public REST API (no MCP).",
  "owner": "portfolio",
  "department": "Product",
  "version": "0.1.0",
  "status": "live-tested",
  "project": "n8n-autonomous-agent",
  "lastWriter": "n8n public REST API",
  "n8nWorkflowId": "fIAHA00y4Rft2BrC",
  "source": "autonomous-agent.workflow.js",
  "canonical": "autonomous-agent.canonical.json",
  "release": "autonomous-agent-v0.1.0.json",
  "request": "autonomous-agent.md",
  "trigger": "webhook (POST) + manual (editor demo)",
  "integrations": [
    "n8n Webhook Trigger (POST /webhook/portfolio/autonomous-agent, responseMode=responseNode)",
    "n8n Manual Trigger (editor demo: a built-in bug task)",
    "Agent Loop Code node -- mirrors scripts/lib/agent-core.mjs (bounded tool-use loop + keyword planner + guardrails)",
    "Tools = the portfolio workflows via the signed interaction-gateway (support-triage / product-feedback / rag / eval / drift); v0.1.0 STUB tools, live gateway execution opt-in",
    "Redacted audit (stopReason + tool sequence + counts only; never the raw task)"
  ],
  "tags": [ "agent", "autonomous", "tool-use-loop", "agent-trajectory-eval", "guardrails", "stub-default", "capstone", "workflow-as-code" ],
  "fixtures": [
    "golden/01-bug-known-issue.json",
    "golden/02-question-grounded.json",
    "golden/03-feedback.json",
    "golden/04-unsafe-injection.json",
    "golden/05-unsafe-destructive.json",
    "golden/06-no-signal.json"
  ],
  "smokeCases": [ "bug-known-issue", "unsafe-injection", "no-signal" ],
  "localOnly": true,
  "buildMechanism": "sdk-standalone (@n8n/workflow-sdk 0.15.1) compiled to JSON via scripts/lib/compile-sdk.mjs (parseWorkflowCode + validateWorkflow). The Agent Loop Code node mirrors agent-core.mjs as inline jsCode; scripts/test-agent-workflow.mjs proves the COMPILED copy offline (golden tasks + a differential asserting byte-identical run records vs the core). Mirrors the n8n-interaction-gateway toolchain + stub-default discipline. Deployed via the n8n public REST API (no MCP).",
  "notes": "v0.1.0 -- the portfolio's NINTH project and its named capstone (autonomous agent). 8 nodes (2 triggers: webhook + manual demo; 1 sticky; 4 code: normalizeTask / agentLoop / buildResponse / buildDemoTask; 1 respondToWebhook). Gates green: verify:agent (59/59 pure core: 6 golden tasks + 3 guardrail negatives + LLM-planner machinery [free-form + classifier]) + verify:workflow (24/24: the COMPILED Agent Loop, 6 golden tasks + a differential vs the core, byte-identical run records) + verify:static + verify:json. THE CAPSTONE THESIS: the SAME scoreTrajectory rubric grades the deterministic stub planner AND a live LLM planner -- proven live via verify:llm (the 'eval a non-deterministic agent' answer A+D were built to de-risk); the agent's tools are the 6 portfolio workflows via the signed gateway. Guardrails proven as REJECTED NEGATIVES: refusal (destructive/prompt-injection -> no tool) / non-allowlisted-tool (never executed) / max-steps (bounded) / no-fabricated-result (tool failure recorded ok:false). DEPLOYED + LIVE-VERIFIED 2026-06-05 as n8n workflow fIAHA00y4Rft2BrC (active): a bug task -> finished with the 2-step trajectory rag->support-triage (finalAnswer synthesises both tool results); an unsafe task (prompt-injection + destructive) -> refused, 0 tool calls (the refusal guardrail fired live). The agent uses NO crypto/require, so it needs no runner env flags. LIVE LLM PLANNER BUILT + RUN 2026-06-05 (verify:llm, Ollama llama3.2:3b, same rubric + stub-vs-LLM calibration): free-form step planning 2/6 (trust LOW -- a small model cannot track multi-step state, re-calls tools to max-steps) vs LLM-classifier + deterministic routeByClass 6/6 (trust HIGH, incl. the 2-step rag->support-triage) -- an honest architecture finding, no rubric loosening. DEFERRED: live gateway tool execution (the agent signs + routes to the deployed gateway instead of stub tools). Never touches the existing workflow ids.",
  "updatedAt": "2026-06-05T00:00:00Z"
}
